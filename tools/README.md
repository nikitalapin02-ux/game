# tools/

- `dechecker_locations.py` — one-off script used to strip the baked-in
  checkerboard background out of `assets/locations/raw/*.png` and write
  true-alpha RGBA versions into `game/assets/locations/`. Kept for
  reference in case new location packs arrive with the same issue.
